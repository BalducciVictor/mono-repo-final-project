module.exports = {
  // Utiliser un seul guillemet pour les chaînes de caractères
  singleQuote: true,
  // Utiliser des points-virgules à la fin des instructions
  semi: true,
  // Utiliser une largeur de ligne maximale de 80 caractères
  printWidth: 80,
  // Utiliser une indentation de 2 espaces
  tabWidth: 2,
  // Utiliser des virgules comme séparateurs dans les objets et tableaux
  trailingComma: 'all',
  // Ne pas ajouter d'espace avant les parenthèses de fonction
  arrowParens: 'avoid',
  // Ne pas formatter automatiquement les fichiers Markdown
  proseWrap: 'never',
};
